/**
 * @param {number} x
 * @param {number} n
 * @return {number}
 */
var myPow = function(x, n) {
//     let res;
//     const helper = (x, n) => {
//         if (x === 0) return 0;
//         if (n === 0) return 1;
        
//         res = helper
//     } 
    return Math.pow(x, n);
};